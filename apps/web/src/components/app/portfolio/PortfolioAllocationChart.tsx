"use client"; // fetches GET /portfolio with the in-memory access token (spec 002 AC41); a Server Component can't attach it

/**
 * AC22a stub — fleshed out with the real pie chart in the AC26 commit.
 */
export function PortfolioAllocationChart() {
  return <p className="text-sm text-ink-muted">Cargando distribución…</p>;
}
