// Re-exports of Prisma-generated model types, per the feature-module
// structure convention (`src/modules/<feature>/entities`). These are not
// hand-declared — Prisma is the single source of truth for the DB shape.
export type {
  User,
  RefreshToken,
  PasswordResetToken,
} from '../../../../generated/prisma/client.js';
